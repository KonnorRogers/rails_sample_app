<%# <%= form_with(model: current_user.active_relationships.build) do |f| %1> %>
<%#   <div><%= hidden_field_tag :followed_id, @user.id %1></div> %>
<%#   <%= f.submit "Follow", class: 'btn btn-primary' %1> %>
<%# <% end %1> %>

<%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "Follow", class: 'btn btn-primary' %>
<% end %>
